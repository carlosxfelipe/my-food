; força node_modules no formato tradicional (compatível com React Native/Metro)
node-linker=hoisted

; só permite instalar versões publicadas há pelo menos 7 dias (10080 minutos)
minimum-release-age=10080
